import { StyleSheet } from "react-native";

export default textStyles = StyleSheet.create({
    default: {
        fontSize: 16,
        color: 'white',
        lineHeight: 17,
    },
    title: {
        fontSize: 24,
        color: 'white',
        fontWeight: 'bold',
    },
    cardTitle: {
        fontSize: 20,
        color: 'white',
        fontWeight: 'bold',
    },
})
