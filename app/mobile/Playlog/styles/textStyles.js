import { StyleSheet } from "react-native";

export default textStyles = StyleSheet.create({
    default: {
        fontSize: 16,
        color: 'white',
    },
    title: {
        fontSize: 24,
        color: 'white',
    },
    cardTitle: {
        fontSize: 20,
        color: 'white',
    },
})
